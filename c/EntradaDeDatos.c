#include <stdio.h>

int main() {
    int num1 = 10;
    int num2 = 20;
    int suma = num1 + num2;
    printf("La suma de %d y %d es %d\n", num1, num2, suma);
    return 0;
} 
## La diferencia es que en c solo pueden declarar una vez las variables 
##a diferencia de phyton que los puedes declarar las veces que quieras
## y se tomara en cuenta la ultima vez